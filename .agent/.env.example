# MCP 環境変数テンプレート
# 必要に応じてこのファイルを .env にコピーして実際の値を入力してください
# 実際の認証情報が入った .env ファイルをバージョン管理にコミットしないでください

# 注意: 現在の mcp.json 設定は GitHub に HTTP を使用しており、
# ほとんどの環境変数は必要ありません。
# このファイルは参考用に保持されていますが、現在は使用されていません。

# 将来的に環境変数を追加する必要がある場合は、ここに追加してください。
# 例:
# SOME_API_KEY=your_api_key_here
