# PDCA Cycle 47 — 2026-04-22

- **Profile**: `experiment_2026-04-22_sl14_bblk3_cmf010_contra30_66` (新規)
- **Parent**: cycle45 combined (`experiment_2026-04-22_sl14_bblk3_cmf010`)
- **Level**: Level 1 (contrarian パラメータ 2 軸)

## 目的

cycle46 で「trailing は dead / rsi_buy_max 短期補強は失敗」と確定したので、
cycle47 は C1 案（contrarian 軸 sweep）で 3m/6m の改善可能性を探る。

Sweep: `contrarian.rsi_entry × contrarian.rsi_exit`

## 実行条件

- 同 cycle45/46 と同じ 4 期間 + WFO 10 windows / 9 cells
- grid: `rsi_entry ∈ {30, 32, 34}` × `rsi_exit ∈ {66, 68, 70}`
- base: combined (`experiment_2026-04-22_sl14_bblk3_cmf010`)

## 結果

### WFO sweep (10 windows × 9 cells)

**Axis clamp discovery**: 
- `rsi_entry ∈ {32, 34}` かつ `rsi_exit ∈ {66, 68}` は bit-identical
- 理由: `stance_rules.rsi_oversold=32` / `rsi_overbought=68` で stance gate が先に発動し、contrarian の entry>=32 / exit<=68 は no-op
- 有効セルは `rsi_entry=30` (3 cells) と `rsi_exit=70` (2 cells)

**IS 勝者頻度**:
| rsi_entry | rsi_exit | 勝者数 |
|---|---|---|
| **30** | 66 | 4 |
| 32 | 66 | 3 |
| **30** | 70 | 2 |
| 32 | 70 | 1 |

**axis 集計**: `rsi_entry=30` が **6/10** で robust 基準達成。

### 4 期間 multi-period (vs combined cycle45)

| 期間 | combined | **contra30_66** | δ |
|---|---|---|---|
| 3m | +0.80% | +0.80% | ≈0 |
| 6m | +2.38% | +1.84% | -0.54pp |
| 1y | +11.73% | +10.99% | -0.74pp |
| 2y | +15.03% | +14.95% | -0.08pp |
| aggregate geomMean | **+7.32%** | +6.98% | -0.34pp |

## 判定

### C1 (contrarian 短期補強) — ❌ reject

- 3m で ≈0 の改善しか出ず、6m/1y/2y いずれも劣化
- aggregate geomMean も -0.34pp で全体悪化
- WFO 6/10 robust だが multi-period 評価では下がる、典型的な IS 最適化/OOS 悪化パターン

### 副次的 learning — ✅ axis 構造の理解

- `signal_rules.contrarian.rsi_entry/exit` は `stance_rules.rsi_oversold/overbought` の閾値に clamp される
- contrarian 軸を動かしたい場合は **stance 側の閾値も同時にずらす必要**
- 今後 contrarian axis を単独で sweep するのは効果が期待できず、stance+contrarian の combined sweep が本命

### 必須制約 — ✅ 全クリア
- MaxDrawdown: 全 4 期間で 7% 以下
- TotalReturn: 全 4 期間で正

## 学び

1. **パラメータ階層の clamp を先に理解すること**: `stance_rules.*` の閾値は `signal_rules.*` の発火条件を先取りしており、signal 側単独で sweep しても値域が狭まって dead cell が生じる。これは cycle43 の "silent no-op" とは別種の「階層 clamp no-op」と呼ぶべき現象で、今後 sweep 前に **axis 実効域** を entity/usecase のコードで確認する手順を加えるべき。
2. **contrarian axis は単独では効果が薄い**: cycle47 では combined 同等以下。Level 2 ロジック組替 (contrarian 無効化 or macd_histogram_limit など別 field) に回すか、stance_rules と合算で攻めるのが筋。
3. **v6 promote 最有力候補は依然 cycle45 combined**: cycle46 (rsi55) と cycle47 (contra30_66) はどちらも combined に劣る。3 サイクル連続で combined を超えられず、短期の 3m/6m 補強は LTC 15m の現行 base family では構造的に難しいと判断。

## 次サイクル提案 (cycle48)

- **D1 (本命)**: stance_rules + contrarian 合成 sweep (`rsi_oversold × contrarian.rsi_entry` を同時に下げる)
- **D2**: breakout 軸 (`volume_ratio_min`) — cycle47 rsi entry と独立なので別軸として試す価値
- **D3 (user 決断)**: 次サイクル探索を停止して cycle45 combined を **v6 として production.json promote** するか。3 サイクル連続で superset が見つからないのは local optimum の兆候で、これ以上の微調整より live 検証 or asset diversification が ROI 高い

## 生成アーティファクト

- 新規 profile: `backend/profiles/experiment_2026-04-22_sl14_bblk3_cmf010_contra30_66.json`
- 結果 JSON: `/tmp/cycle47_wfo.json`, `/tmp/cycle47_contra30_66.json`
- pdcaCycleId (DB): `2026-04-22_cycle47_contrarian`, `_contra30_66`
